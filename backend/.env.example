DATABASE_URL=postgresql://user:password@host:5432/watchlist
JWT_SECRET=change-me-to-a-long-random-string
CORS_ORIGINS=http://localhost:5173,https://your-frontend.vercel.app
COOKIE_SECURE=true
COOKIE_SAMESITE=none

# Optional: enables the LLM rewrite of the digest narrative. If unset, a
# rule-based template is used instead (fully functional either way).
ANTHROPIC_API_KEY=
